{
  "$id": "https://foundry-dev.com/schemas/foundry.schema.json",
  "$schema": "https://json-schema.org/draft-07/schema#",

  "title": "Foundry Platform Manifest",
  "description": "Declarative manifest for a Foundry-managed platform.",

  "schemaVersion": "0.1.0",

  "required": ["$schema", "schemaVersion", "name", "ci"],

  "properties": {
    "$schema": {
      "description": "URI of the JSON Schema used to validate this manifest.",
      "type": "string",
      "format": "uri"
    },
    "schemaVersion": {
      "description": "Semantic version of the Foundry manifest spec this file targets.",
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "name": {
      "description": "Platform identifier (slug).",
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]{1,63}$"
    },
    "ci": {
      "description": "Continuous Integration and infrastructure orchestration configuration.",
      "required": ["orchestration", "provider"],
      "properties": {
        "orchestration": {
          "description": "Tool used for infrastructure orchestration.",
          "enum": ["terraform"]
        },
        "provider": {
          "description": "Primary infrastructure provider for this platform.",
          "enum": ["aws"]
        }
      }
    }
  }
}
