{
  "$id": "https://foundry-dev.com/schemas/foundry.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",

  "title": "Foundry Platform Manifest",
  "description": "Declarative manifest for a Foundry-managed platform.",

  "schemaVersion": "0.1.0",

  "required": [
    "$schema",
    "schemaVersion",
    "name",
    "repository",
    "ci"
  ],

  "properties": {
    "$schema": {
      "description": "URI for the JSON Schema used to validate this manifest.",
      "type": "string",
      "format": "uri"
    },

    "schemaVersion": {
      "description": "Version of the Foundry manifest specification.",
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },

    "name": {
      "description": "Human-readable name of the platform.",
      "type": "string",
      "minLength": 1
    },

    "repository": {
      "description": "Name of the root repository folder for this platform (used by Foundry CLI).",
      "type": "string",
      "pattern": "^[a-z0-9-]+$"
    },

    "servicesDir": {
      "description": "Relative path to the directory containing runnable services (default: 'apps').",
      "type": "string",
      "default": "apps"
    },

    "services": {
      "description": "Launch configurations for discovered services. Keys are service names (directory names).",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/serviceConfig"
      }
    },

    "ci": {
      "description": "Continuous Integration and infrastructure orchestration configuration.",

      "required": [
        "orchestration",
        "provider"
      ],

      "properties": {
        "orchestration": {
          "description": "Tool used for infrastructure orchestration.",
          "enum": ["terraform"]
        },

        "provider": {
          "description": "Primary infrastructure provider for this platform.",
          "enum": ["aws"]
        }
      }
    }
  },

  "definitions": {
    "serviceConfig": {
      "type": "object",
      "description": "Launch configuration for a single service.",
      "properties": {
        "enabled": {
          "description": "Whether this service should be started by `foundry run`. Defaults to true.",
          "type": "boolean",
          "default": true
        },
        "port": {
          "description": "The primary port the service listens on.",
          "type": "integer",
          "minimum": 1,
          "maximum": 65535
        },
        "actuatorPort": {
          "description": "The port for health/actuator endpoints (Spring Boot). Defaults to port if not set.",
          "type": "integer",
          "minimum": 1,
          "maximum": 65535
        },
        "args": {
          "description": "Additional command-line arguments to pass to the service start command.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "env": {
          "description": "Environment variables to set when running the service.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "sshTunnel": {
          "description": "SSH tunnel configuration to establish before starting the service (e.g., for database access through an SSH jump host).",
          "$ref": "#/definitions/sshTunnelConfig"
        }
      },
      "additionalProperties": false
    },
    "sshTunnelConfig": {
      "type": "object",
      "description": "Configuration for an SSH tunnel through a jump host.",
      "required": ["localPort", "remoteHost", "remotePort", "host"],
      "properties": {
        "localPort": {
          "description": "Local port to bind for the tunnel (e.g., 3306 for MySQL).",
          "type": "integer",
          "minimum": 1,
          "maximum": 65535
        },
        "remoteHost": {
          "description": "Remote host to tunnel to (e.g., database hostname like 'mydb.abc123.us-east-2.rds.amazonaws.com').",
          "type": "string"
        },
        "remotePort": {
          "description": "Remote port to tunnel to (e.g., 3306 for MySQL, 5432 for PostgreSQL).",
          "type": "integer",
          "minimum": 1,
          "maximum": 65535
        },
        "host": {
          "description": "SSH host to connect through (IP or hostname).",
          "type": "string"
        },
        "user": {
          "description": "SSH user for the host. Defaults to 'ec2-user'.",
          "type": "string",
          "default": "ec2-user"
        },
        "password": {
          "description": "Path to SSH private key file. Can be absolute or relative to the foundry.json manifest.",
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  }
}
